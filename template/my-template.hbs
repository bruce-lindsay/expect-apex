public class Expect{{datatype}} {
    //probably we can use this string for the other implementations as well?
    public static final String {{upper datatype}}_SHOULD_BE_MESSAGE = 'Expected {0} should{1} be {2}';
    public static final String {{upper datatype}}_SHOULD_BE_LESS_MESSAGE = 'Expected {0} should be less than {1}';
    public static final String {{upper datatype}}_SHOULD_BE_LESS_OR_EQUAL_MESSAGE = 'Expected {0} should be less than or equal to {1}';
    public static final String {{upper datatype}}_SHOULD_BE_GREATER_MESSAGE = 'Expected {0} should be greater than {1}';
    public static final String {{upper datatype}}_SHOULD_BE_GREATER_OR_EQUAL_MESSAGE = 'Expected {0} should be greater than or equal to {1}';
    public static final String {{upper datatype}}_BETWEEN_MESSAGE = 'Expected {0} should be between {1} and {2}';

    private final {{datatype}} expected;

    public Expect{{datatype}}({{datatype}} expected) {
        this.expected = expected;
    }
    private Expect{{datatype}} assertThis(Boolean test, String message) {
        Expect.assert(test, message);
        return this;
    }
    public Expect{{datatype}} shouldEqual({{datatype}} actual) {
        return shouldEqual(actual, String.format({{upper datatype}}_SHOULD_BE_MESSAGE, new String[]{
            String.valueOf(expected), '', String.valueOf(actual)}));
    }
    public Expect{{datatype}} shouldEqual({{datatype}} actual, String message) {
        return assertThis(expected == actual, message);
    }
    public Expect{{datatype}} shouldNotEqual({{datatype}} actual) {
        return shouldNotEqual(actual, String.format({{upper datatype}}_SHOULD_BE_MESSAGE, new String[]{
            String.valueOf(expected), ' not', String.valueOf(actual)}));
    }
    public Expect{{datatype}} shouldNotEqual({{datatype}} actual, String message) {
        return assertThis(expected != actual, message);
    }
    public Expect{{datatype}} shouldBeLessThan({{datatype}} actual, string message) {
        return assertThis(expected < actual, message);
    }
    public Expect{{datatype}} shouldBeLessThan({{datatype}} actual) {
        return shouldBeLessThan(actual, String.format({{upper datatype}}_SHOULD_BE_LESS_MESSAGE, new String[]{
            String.valueOf(expected),
            String.valueOf(actual)
        }));
    }
    public Expect{{datatype}} shouldBeLessThanOrEqualTo({{datatype}} actual, string message) {
        return assertThis(expected <= actual, message);
    }
    public Expect{{datatype}} shouldBeLessThanOrEqualTo({{datatype}} actual) {
        return shouldBeLessThanOrEqualTo(actual, String.format({{upper datatype}}_SHOULD_BE_LESS_OR_EQUAL_MESSAGE, new String[]{
            String.valueOf(expected),
            String.valueOf(actual)
        }));
    }
    public Expect{{datatype}} shouldBeGreaterThan({{datatype}} actual, string message) {
        return assertThis(expected > actual, message);
    }
    public Expect{{datatype}} shouldBeGreaterThan({{datatype}} actual) {
        return shouldBeGreaterThan(actual, String.format({{upper datatype}}_SHOULD_BE_GREATER_MESSAGE, new String[]{
            String.valueOf(expected),
            String.valueOf(actual)
        }));
    }
    public Expect{{datatype}} shouldBeGreaterThanOrEqualTo({{datatype}} actual, string message) {
        return assertThis(expected >= actual, message);
    }
    public Expect{{datatype}} shouldBeGreaterThanOrEqualTo({{datatype}} actual) {
        return shouldBeGreaterThanOrEqualTo(actual, String.format({{upper datatype}}_SHOULD_BE_GREATER_OR_EQUAL_MESSAGE, new String[]{
            String.valueOf(expected),
            String.valueOf(actual)
        }));
    }
    public Expect{{datatype}} shouldBeBetween({{datatype}} rangeStart, {{datatype}} rangeEnd) {
        return shouldBeBetween(rangeStart, rangeEnd,
            String.format({{upper datatype}}_BETWEEN_MESSAGE, new String[]{
                String.valueOf(expected),
                String.valueOf(rangeStart),
                String.valueOf(rangeEnd)
            }));
    }
    public Expect{{datatype}} shouldBeBetween({{datatype}} rangeStart, {{datatype}} rangeEnd, string message) {
        Boolean testedBetween = (rangeStart < rangeEnd) ?
            rangeStart <= this.expected && this.expected <= rangeEnd :
            rangeEnd <= this.expected && this.expected <= rangeStart;

        return assertThis(testedBetween, message);
    }
    public Expect{{datatype}} andIt { get { return this; } }
}